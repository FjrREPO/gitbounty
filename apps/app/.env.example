# GitBounty API — server-side GitHub cache, keeps the browser off GitHub's rate limit
NEXT_PUBLIC_API_URL=http://localhost:8080

# Goldsky subgraph; falls back to reading the escrow over RPC when empty
NEXT_PUBLIC_SUBGRAPH_URL=

NEXT_PUBLIC_COSTON2_RPC=https://coston2-api.flare.network/ext/C/rpc

# WalletConnect / Reown project id
NEXT_PUBLIC_REOWN_PROJECT_ID=
